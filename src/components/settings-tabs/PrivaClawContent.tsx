// PrivaClawContent: the full SkillConfig page content, without the standalone page shell.
// We just re-export the page — it renders its own header internally which is fine inside a tab.
export { default } from "@/pages/SkillConfig";
